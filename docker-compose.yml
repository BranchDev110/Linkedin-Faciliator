services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    # Secrets/URLs come from env_file (not interpolated environment), so an empty
    # host shell var cannot wipe values from deploy/env/production.env.
    env_file:
      - path: .env
        required: false
    environment:
      NODE_ENV: production
      PORT: 3001
      # Always use the compose network hostname — never host .env's 127.0.0.1.
      MONGODB_URI: mongodb://mongo:27017/li-facilitator
      STORAGE_ROOT: /app/storage
    expose:
      - "3001"
    volumes:
      - app_storage:/app/storage
    networks:
      - lif

  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db
    networks:
      - lif
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - "${APP_PORT:-3001}:443"
    volumes:
      - ${NGINX_CONF:-./deploy/nginx/li-facilitator-https.conf}:/etc/nginx/conf.d/default.conf:ro
      - ${TLS_PATH:-./deploy/certs}:/etc/nginx/certs:ro
    networks:
      - lif

volumes:
  app_storage:
  mongo_data:

networks:
  lif:
    driver: bridge
